{
  "name": "@scouting-platform/worker",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
