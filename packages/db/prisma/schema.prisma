generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// Initial schema starts empty on purpose.
/// Create the first real migration from issue SP-005.
